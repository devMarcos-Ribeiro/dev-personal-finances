{
  "name": "dev-personal-finances",
  "version": "1.0.0",
  "description": "A project to control all my finances at the end of the month",
  "main": "./src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "start": "npm run build && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "build": "rimraf ./build && tsc",
  "dependencies": {
    "express": "^4.17.3"
  }
}
